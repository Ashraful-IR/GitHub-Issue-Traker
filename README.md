
    - 1️⃣ What is the difference between var, let, and const?
    Ans: 
    Var -allow redeclaration and update within same block
    Valet -allow update but no redeclaration within same block
    const- cannot be reassigned after initial  assignment

    - 2️⃣ What is the spread operator (...)?
    Ans:Take everything inside this thing and spread it out.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans:
    forEach() -runs a function for every element, but it does not return a new array.
    map() -takes every element, transforms it, and returns a new array.
    filter() -keeps elements that pass a condition.

    - 4️⃣ What is an arrow function?
    Ans: An arrow function is a shorter, cleaner way to write functions in JavaScript.

    - 5️⃣ What are template literals?
    Ans:Template literals are a JavaScript feature for creating strings using backticks (` `) instead of quotes
